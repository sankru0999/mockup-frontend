{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npx vite build --config vite.config.production.ts"
  },
  "deploy": {
    "startCommand": "npx serve -s dist -l $PORT",
    "healthcheckPath": "/"
  }
}